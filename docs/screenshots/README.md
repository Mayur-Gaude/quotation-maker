# Screenshots (optional)

Add PNG or WebP images here and reference them from the root `README.md`, for example:

- `dashboard.png` — quotation list + summary cards  
- `create-quotation.png` — create form  
- `item-library.png` — saved presets  
- `pdf-sample.png` — optional: exported PDF preview  

Keep file sizes reasonable (compress before commit if you add many images).
